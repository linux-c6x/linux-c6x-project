# makefile fragment to create a specific bootblob

EVM=evmc6472
FSTYPE=NFS
BASEFS=ltp-root
SYSLINK_TYPE=all
TEST_MODULES=yes
source ${STD_TEMPLATE_DIR}/defs/${EVM}.sh
DESC="Testing NFS dir or archive + Kernel for $EVM w/ adjusted command line"
